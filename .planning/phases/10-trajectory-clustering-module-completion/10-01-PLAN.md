---
phase: 10-trajectory-clustering-module-completion
plan: 01
type: execute
wave: 1
depends_on:
  - 07-03
files_modified:
  - demo-web/src/platform/pages/ClusteringPage.tsx
  - demo-web/src/platform/clustering/clusteringTypes.ts
  - demo-web/src/platform/clustering/clusteringViewModel.ts
  - demo-web/src/App.css
  - demo-web/package.json
  - demo-web/tests/clustering-module.test.mjs
autonomous: true
requirements:
  - CLUS-01
  - CLUS-02
  - CLUS-04
  - BASE-03
must_haves:
  truths:
    - Phase 6 already exported `clustering-summary.json`, `clustering-stage-previews.json`, `clustering-corridor-runtime.json`, and `clustering-corridor-review.json`, so 10-01 should turn those committed artifacts into a real module instead of reopening notebook-side export work.
    - The first honest Phase 10 delivery must let a viewer switch across raw, segmented, compressed, runtime-corridor, and review-corridor layers while keeping noise re-clustering explicitly deferred rather than fabricated.
    - The corridor runtime layer already points at the same `main-corridor-tracks.json` dataset used by RouteEditor, so the clustering page should expose that handoff as a product-facing bridge.
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - demo-web/public/data/modules/clustering/clustering-bundle.json
    - demo-web/public/data/modules/clustering/clustering-summary.json
    - demo-web/public/data/modules/clustering/clustering-stage-previews.json
    - demo-web/public/data/modules/clustering/clustering-corridor-runtime.json
    - demo-web/public/data/modules/clustering/clustering-corridor-review.json
    - demo-web/src/platform/pages/ClusteringPage.tsx
  key_links:
    - `clustering-bundle.json` already exposes summary, stage-preview, runtime-corridor, and review-corridor entry files.
    - `clustering-summary.json` already exposes stage counts, review status, and the deferred noise re-clustering boundary.
    - `clustering-corridor-runtime.json` matches the RouteEditor runtime dataset shape closely enough to support an explicit corridor handoff without inventing a parallel map entity model.
---

<objective>
Turn the Phase 6 clustering exports into the first real Phase 10 module surface by adding layer switching, clustering statistics, corridor review comparison, and explicit RouteEditor linkage.

Purpose: move the clustering page out of placeholder territory so the website can present the raw-to-corridor pipeline as a coherent product story without pretending the deferred noise re-clustering path is already stable.
Output: a clustering module that is interactive, traceable to committed artifacts, and covered by module-specific smoke checks.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@demo-web/public/data/modules/clustering/clustering-bundle.json
@demo-web/public/data/modules/clustering/clustering-summary.json
@demo-web/public/data/modules/clustering/clustering-stage-previews.json
@demo-web/public/data/modules/clustering/clustering-corridor-runtime.json
@demo-web/public/data/modules/clustering/clustering-corridor-review.json
@demo-web/src/platform/pages/ClusteringPage.tsx
@demo-web/src/RouteEditor.tsx
@demo-web/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Phase 10 clustering layer-switching and preview contract</name>
  <files>demo-web/src/platform/clustering/clusteringTypes.ts, demo-web/src/platform/clustering/clusteringViewModel.ts, demo-web/src/platform/pages/ClusteringPage.tsx</files>
  <action>Create a clustering view-model boundary that combines the summary bundle, stage previews, runtime corridor export, and review corridor export into one page contract. The page must let a viewer switch among raw, segmented, compressed, runtime corridor, and review corridor layers while keeping the selected layer readable through stats, preview traces, and honest layer descriptions.</action>
  <verify>rg "Layer Switcher|raw|segmented|compressed|corridor" demo-web/src/platform/pages/ClusteringPage.tsx demo-web/src/platform/clustering</verify>
  <done>The clustering page exposes a true phase-level layer-switching experience instead of a static stage-count list.</done>
</task>

<task type="auto">
  <name>Task 2: Add cluster statistics, corridor review comparison, and RouteEditor linkage</name>
  <files>demo-web/src/platform/pages/ClusteringPage.tsx, demo-web/src/App.css</files>
  <action>Extend the clustering page so it surfaces cluster-count statistics, selected corridor details, runtime-versus-review comparison, and one explicit bridge into the shared RouteEditor runtime. Keep the copy honest that noise re-clustering remains deferred because the missing artifact path is not stable in the current environment.</action>
  <verify>rg "Cluster Statistics|Runtime vs Review|RouteEditor|Noise re-clustering" demo-web/src/platform/pages/ClusteringPage.tsx demo-web/src/App.css</verify>
  <done>The module can explain corridor counts, review status, and cross-module entity linkage without needing notebook access.</done>
</task>

<task type="auto">
  <name>Task 3: Add clustering-specific smoke coverage and wire it into the normal smoke command</name>
  <files>demo-web/tests/clustering-module.test.mjs, demo-web/package.json</files>
  <action>Add a clustering-focused test that asserts the shipped bundle contract, available preview layers, corridor comparison invariants, deferred noise handling, and the presence of the new UI affordances. Register it in the standard smoke script alongside the existing shell, forecast, and repair tests.</action>
  <verify>node --test demo-web/tests/clustering-module.test.mjs</verify>
  <done>The clustering module has repeatable module-specific coverage instead of relying only on shell smoke tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg "Layer Switcher|raw|segmented|compressed|corridor" demo-web/src/platform/pages/ClusteringPage.tsx demo-web/src/platform/clustering`
- [ ] `rg "Cluster Statistics|Runtime vs Review|RouteEditor|Noise re-clustering" demo-web/src/platform/pages/ClusteringPage.tsx demo-web/src/App.css`
- [ ] `node --test demo-web/tests/clustering-module.test.mjs`
- [ ] `npm run smoke`
</verification>

<success_criteria>

- The clustering page now supports real layer switching across the shipped raw, segmented, compressed, runtime-corridor, and review-corridor exports
- Cluster statistics and review comparison are visible without fabricating notebook-only noise outputs
- The page clearly links the corridor runtime layer back to the RouteEditor dataset used elsewhere in the site
- Clustering-specific smoke coverage is part of the normal verification path

</success_criteria>

<output>
After completion, create `.planning/phases/10-trajectory-clustering-module-completion/10-01-SUMMARY.md`
</output>
