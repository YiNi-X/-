---
phase: 06-data-completion-and-lineage
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - demo-web/scripts/phase6_bundle_common.py
  - demo-web/scripts/export_phase6_primary_bundles.py
  - demo-web/public/data/modules/artifact-index.json
  - demo-web/public/data/modules/forecast
  - demo-web/public/data/modules/repair
autonomous: true
requirements:
  - BASE-02
  - BASE-05
must_haves:
  truths:
    - Forecast and repair data can be regenerated into committed website-facing module packages without using notebooks as the execution interface.
    - Forecast and repair packages each expose lineage metadata and stable runtime files from one module directory.
    - The module artifact index points downstream website code at committed package locations instead of ad hoc file discovery.
  artifacts:
    - demo-web/scripts/phase6_bundle_common.py
    - demo-web/scripts/export_phase6_primary_bundles.py
    - demo-web/public/data/modules/artifact-index.json
    - demo-web/public/data/modules/forecast
    - demo-web/public/data/modules/repair
  key_links:
    - The primary exporter consumes the authoritative sources documented in `06-LINEAGE-MATRIX.md` and writes forecast and repair packages plus their manifests.
    - `artifact-index.json` registers the forecast and repair module package locations so later frontend contract work can load them consistently.
---

<objective>
Export the primary website-facing module bundles for flow prediction and trajectory repair.

Purpose: move the two strongest offline algorithm areas onto a committed, reproducible module-package surface that later frontend work can consume.
Output: shared Phase 6 bundle helpers, one primary export command, one artifact index, and committed forecast and repair package directories under `demo-web/public/data/modules/`.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-completion-and-lineage/06-CONTEXT.md
@.planning/phases/06-data-completion-and-lineage/06-RESEARCH.md
@.planning/phases/06-data-completion-and-lineage/06-ASSET-INVENTORY.md
@.planning/phases/06-data-completion-and-lineage/06-LINEAGE-MATRIX.md
@demo-web/scripts/generate_first_version_data.py
@demo-web/src/runtimeData.ts
@demo-web/src/runtimeSchemas.ts
@代码依据/流量预测/
@代码依据/轨迹修复/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Phase 6 bundle helpers and a committed artifact index</name>
  <files>demo-web/scripts/phase6_bundle_common.py, demo-web/scripts/export_phase6_primary_bundles.py, demo-web/public/data/modules/artifact-index.json</files>
  <action>Create a shared Python helper layer for writing Phase 6 module manifests, stable bundle metadata, output directories, and dry-run reporting. Add a primary exporter entrypoint that can target forecast or repair generation from the authoritative sources captured in the lineage matrix. Introduce one committed `artifact-index.json` at `demo-web/public/data/modules/` that records the shipped module package roots and their main manifest or bundle files.</action>
  <verify>python demo-web/scripts/export_phase6_primary_bundles.py --help</verify>
  <done>The repo has one reusable export helper surface and one artifact index for primary module packages.</done>
</task>

<task type="auto">
  <name>Task 2: Export the forecast module package from authoritative flow inputs</name>
  <files>demo-web/scripts/phase6_bundle_common.py, demo-web/scripts/export_phase6_primary_bundles.py, demo-web/public/data/modules/forecast, demo-web/public/data/modules/artifact-index.json</files>
  <action>Export the forecast module package as one directory containing a manifest plus the runtime files required for later model or horizon switching. Use the flow research inputs and existing runtime patterns as the upstream source of truth, not ad hoc notebook HTML. Include lineage metadata, scenario metadata, and the fields needed for Phase 8 to compare STGCN, LSTM, and BiLSTM across horizons. If authoritative LSTM or BiLSTM exports are not yet available, mark them as deferred in the forecast package metadata rather than shipping placeholder or stubbed runtime results.</action>
  <verify>python demo-web/scripts/export_phase6_primary_bundles.py --module forecast --dry-run</verify>
  <done>The forecast package can be regenerated offline and advertises its lineage and runtime bundle contents from one stable module directory.</done>
</task>

<task type="auto">
  <name>Task 3: Export the repair module package from curated samples and metrics</name>
  <files>demo-web/scripts/phase6_bundle_common.py, demo-web/scripts/export_phase6_primary_bundles.py, demo-web/public/data/modules/repair, demo-web/public/data/modules/artifact-index.json</files>
  <action>Export the repair module package as one directory containing a manifest plus the runtime files required for sample selection, model switching, repaired trajectory display, error charts, and metric summaries. Curate the sample set from the existing repair CSV, PKL, and HTML-era outputs, but ensure the website-facing package is a clean structured export rather than a notebook-side artifact dump. Keep lineage metadata granular enough to trace each exported sample or metric set back to its upstream research files.</action>
  <verify>python demo-web/scripts/export_phase6_primary_bundles.py --module repair --dry-run</verify>
  <done>The repair package can be regenerated offline from curated authoritative inputs and exposes structured runtime files rather than notebook-only outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python demo-web/scripts/export_phase6_primary_bundles.py --help`
- [ ] `python demo-web/scripts/export_phase6_primary_bundles.py --module forecast --dry-run`
- [ ] `python demo-web/scripts/export_phase6_primary_bundles.py --module repair --dry-run`
</verification>

<success_criteria>

- Forecast and repair module packages exist under `demo-web/public/data/modules/`
- The artifact index can point future frontend loaders at those package locations
- Both module packages record authoritative lineage and can be regenerated offline from documented sources
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-completion-and-lineage/06-02-SUMMARY.md`
</output>
