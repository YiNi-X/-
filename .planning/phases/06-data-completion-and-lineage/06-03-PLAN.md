---
phase: 06-data-completion-and-lineage
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - demo-web/scripts/export_phase6_secondary_bundles.py
  - demo-web/scripts/verify_phase6_bundles.py
  - demo-web/public/data/modules/artifact-index.json
  - demo-web/public/data/modules/clustering
  - demo-web/public/data/modules/evaluation
  - demo-web/public/data/modules/overview
  - demo-web/analysis/review
  - demo-web/package.json
  - .planning/phases/06-data-completion-and-lineage/06-RERUN-TIMES.md
  - .planning/phases/06-data-completion-and-lineage/06-DEFERRED-ITEMS.md
autonomous: true
requirements:
  - BASE-02
  - BASE-05
  - BASE-06
must_haves:
  truths:
    - Clustering, evaluation, and overview each have a committed Phase 6 package or an explicitly documented deferred boundary instead of silent gaps.
    - The repo has one repeatable Phase 6 entrypoint that checks bundle contracts and then runs the existing frontend lint/test/build gate before demoing.
    - Manual review images and rerun-time notes exist for the required Phase 6 data surfaces, so schema-valid exports are also visually and operationally auditable.
  artifacts:
    - demo-web/scripts/export_phase6_secondary_bundles.py
    - demo-web/scripts/verify_phase6_bundles.py
    - demo-web/public/data/modules/artifact-index.json
    - demo-web/public/data/modules/clustering
    - demo-web/public/data/modules/evaluation
    - demo-web/public/data/modules/overview
    - demo-web/analysis/review
    - demo-web/package.json
    - .planning/phases/06-data-completion-and-lineage/06-RERUN-TIMES.md
    - .planning/phases/06-data-completion-and-lineage/06-DEFERRED-ITEMS.md
  key_links:
    - The secondary exporter preserves clustering's raw/segmented/compressed/corridor lineage and uses review-first handling for corridor-sensitive outputs.
    - Evaluation and overview packages derive only from exported module bundles, not directly from raw research files, and all secondary packages are registered in `artifact-index.json`.
    - The Phase 6 verification entrypoint runs the package validator first and then the existing frontend verify surface before later phases depend on the new data.
---

<objective>
Finish Phase 6 by exporting the remaining module packages and proving the whole data layer is auditable.

Purpose: close the data-first phase with clustering/evaluation/overview packages, repeatable verification, manual review artifacts, rerun-time notes, and an explicit deferred-items record.
Output: one secondary exporter, one verification entrypoint, an updated artifact index, committed secondary module packages, required manual review images, and Phase 6 evidence docs.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-completion-and-lineage/06-CONTEXT.md
@.planning/phases/06-data-completion-and-lineage/06-RESEARCH.md
@.planning/phases/06-data-completion-and-lineage/06-ASSET-INVENTORY.md
@.planning/phases/06-data-completion-and-lineage/06-LINEAGE-MATRIX.md
@.planning/phases/06-data-completion-and-lineage/06-02-PLAN.md
@demo-web/scripts/extract_main_corridors_from_clustered_ais.py
@demo-web/src/runtimeData.ts
@demo-web/src/runtimeSchemas.ts
@代码依据/轨迹聚类/
@demo-web/public/data/modules/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the clustering package with layered lineage and review-first corridor handling</name>
  <files>demo-web/scripts/export_phase6_secondary_bundles.py, demo-web/public/data/modules/clustering, demo-web/analysis/review</files>
  <action>Export the clustering module package from the layered authoritative sources defined in the context: raw `cleaned_ais.CSV`, segmented `segments(60-90).pkl`, compressed `compressed_segments(60,90,0.03).pkl`, and corridor/exported `main-corridor-tracks.json`. Preserve that lineage in the package manifest and generate the required manual review images for compressed and corridor views. Do not overwrite the official corridor runtime output during this phase unless the review-first comparison path explicitly confirms it is safe to do so.</action>
  <verify>python demo-web/scripts/export_phase6_secondary_bundles.py --module clustering --dry-run</verify>
  <done>The clustering package is shippable with layered provenance and required review artifacts even if noise re-clustering remains deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Export evaluation and overview packages from module bundles, not raw research files</name>
  <files>demo-web/scripts/export_phase6_secondary_bundles.py, demo-web/public/data/modules/artifact-index.json, demo-web/public/data/modules/evaluation, demo-web/public/data/modules/overview</files>
  <action>Export the evaluation and overview packages from the Phase 6 module bundles and their summary-level metadata rather than directly from scattered research files. The evaluation package should unify metric surfaces that are already supported by the exported forecast and repair data. The overview package should summarize module entry points, scale, and business-loop framing using committed module artifacts. Register both secondary packages in `artifact-index.json` so downstream loading has one complete discovery surface. If any metric or storyline cannot be supported yet because an upstream bundle is missing, record that limitation through package metadata and the deferred-items document instead of silently fabricating values.</action>
  <verify>python demo-web/scripts/export_phase6_secondary_bundles.py --module evaluation --dry-run && python demo-web/scripts/export_phase6_secondary_bundles.py --module overview --dry-run</verify>
  <done>Evaluation and overview have committed package directories derived from exported module bundles, the shared artifact index includes them, and any unsupported surfaces are honestly marked as deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Add repeatable validation, rerun-time notes, and deferred-item tracking</name>
  <files>demo-web/scripts/verify_phase6_bundles.py, demo-web/package.json, .planning/phases/06-data-completion-and-lineage/06-RERUN-TIMES.md, .planning/phases/06-data-completion-and-lineage/06-DEFERRED-ITEMS.md, demo-web/analysis/review</files>
  <action>Create one verification surface for Phase 6 by keeping the bundle validator in `verify_phase6_bundles.py` and exposing a single frontend entrypoint such as `npm run verify:phase6` in `demo-web/package.json`. The validator should check required lineage fields, referenced-file existence, finite JSON values, and the most important package invariants such as frame or timeline alignment and corridor count consistency. The single Phase 6 entrypoint must run bundle validation first and then the existing frontend lint/test/build gate so Phase 6 closes with both data-contract confidence and build-entry confidence. Record rerun times for regenerated artifacts in one checked document, and record all deferred items with name, missing reason, and later dependency chain in another. Ensure the required review images exist for forecast, repair, and clustering before closing the phase.</action>
  <verify>npm run verify:phase6</verify>
  <done>The repo has one repeatable Phase 6 verification entrypoint plus checked rerun-time and deferred-item evidence documents.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python demo-web/scripts/export_phase6_secondary_bundles.py --module clustering --dry-run`
- [ ] `python demo-web/scripts/export_phase6_secondary_bundles.py --module evaluation --dry-run`
- [ ] `python demo-web/scripts/export_phase6_secondary_bundles.py --module overview --dry-run`
- [ ] `npm run verify:phase6`
</verification>

<success_criteria>

- Clustering, evaluation, and overview have committed package directories or explicit deferred metadata
- The repo exposes one repeatable verification entrypoint for Phase 6 bundles plus frontend build gates
- Manual review images, rerun times, and deferred-item docs exist as Phase 6 evidence
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-completion-and-lineage/06-03-SUMMARY.md`
</output>
