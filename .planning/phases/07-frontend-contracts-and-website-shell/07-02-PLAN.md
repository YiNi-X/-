---
phase: 07-frontend-contracts-and-website-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - demo-web/src/platform/moduleLoader.ts
  - demo-web/src/platform/usePlatformShell.ts
  - demo-web/src/platform/PlatformShell.tsx
  - demo-web/src/platform/PlatformStatusSurface.tsx
  - demo-web/src/datasetCatalog.ts
  - demo-web/tests/platform-runtime.test.mjs
autonomous: true
requirements:
  - BASE-03
  - BASE-04
must_haves:
  truths:
    - The shell can discover modules from the registry and lazy-load one module package at a time instead of booting every bundle during homepage load.
    - Shell-level state keeps active module, selected dataset or scenario, and module load status coherent across navigation without leaking dashboard-only state into unrelated pages.
    - Shared loading, unavailable, error, and deferred-state surfaces are implemented once and reused across module destinations.
  artifacts:
    - demo-web/src/platform/moduleLoader.ts
    - demo-web/src/platform/usePlatformShell.ts
    - demo-web/src/platform/PlatformShell.tsx
    - demo-web/src/platform/PlatformStatusSurface.tsx
    - demo-web/src/datasetCatalog.ts
    - demo-web/tests/platform-runtime.test.mjs
  key_links:
    - The shell runtime consumes the registry/contracts produced by 07-01 rather than re-reading bundle paths ad hoc.
    - Dataset persistence reuses the existing dataset-catalog URL/local-storage pattern where it belongs at shell scope.
    - Lazy-loading behavior fetches only the selected module's manifest and bundle resources, satisfying Phase 7's payload-splitting requirement.
---

<objective>
Implement the shared shell runtime that discovers, validates, lazy-loads, and caches module data across the new website shell.

Purpose: create one reusable runtime layer for navigation, module status, loading/error behavior, and dataset persistence before building baseline module pages.
Output: shell runtime hooks, module loader utilities, shared status surfaces, and targeted tests covering lazy loading and failure handling.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-contracts-and-website-shell/07-CONTEXT.md
@.planning/phases/07-frontend-contracts-and-website-shell/07-RESEARCH.md
@.planning/phases/07-frontend-contracts-and-website-shell/07-01-PLAN.md
@demo-web/src/runtimeData.ts
@demo-web/src/dashboard/useDashboardRuntime.ts
@demo-web/src/dashboard/DashboardStatusScreen.tsx
@demo-web/src/datasetCatalog.ts
@demo-web/public/data/modules/artifact-index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement registry-driven module loading and cache behavior</name>
  <files>demo-web/src/platform/moduleLoader.ts, demo-web/src/platform/usePlatformShell.ts, demo-web/tests/platform-runtime.test.mjs</files>
  <action>Create the module-loader utilities that fetch the artifact index, selected module manifest, and selected module bundle on demand using the validated runtime-contract layer from 07-01. Add module-level caching and explicit load-status transitions so module pages can reuse data across navigation without forcing a full reload every time the user switches destinations.</action>
  <verify>node --test demo-web/tests/platform-runtime.test.mjs</verify>
  <done>The shell can load one module package at a time and track its runtime status through one tested loader surface.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared shell state for active module, selected dataset, and shared status surfaces</name>
  <files>demo-web/src/platform/usePlatformShell.ts, demo-web/src/platform/PlatformShell.tsx, demo-web/src/platform/PlatformStatusSurface.tsx, demo-web/src/datasetCatalog.ts</files>
  <action>Build the shell-level state model that keeps active module, selected dataset, and module status coherent across navigation. Reuse the repo's existing dataset persistence pattern where it belongs, classify which state stays shell-wide versus page-local, and expose one shared status surface for loading, unavailable, error, and deferred conditions so later module pages do not reinvent those states independently.</action>
  <verify>rg "activeModule|selectedDataset|deferred|unavailable|error" demo-web/src/platform/usePlatformShell.ts demo-web/src/platform/PlatformStatusSurface.tsx</verify>
  <done>There is one reusable shell runtime for navigation state, dataset persistence, and polished runtime-status rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Prove lazy-loading and failure handling with focused runtime tests</name>
  <files>demo-web/tests/platform-runtime.test.mjs, demo-web/src/platform/moduleLoader.ts, demo-web/src/platform/PlatformStatusSurface.tsx</files>
  <action>Add targeted tests that cover the happy path for registry-driven module loading, deferred metadata propagation, malformed manifest or bundle failures, and shell behavior when only the selected module should load. Make those tests the guardrail for BASE-03 and BASE-04 before any page-composition work lands.</action>
  <verify>npm run lint</verify>
  <done>The new shell runtime is defended by focused tests for module discovery, lazy loading, and shared failure semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test demo-web/tests/platform-runtime.test.mjs`
- [ ] `rg "activeModule|selectedDataset|deferred|unavailable|error" demo-web/src/platform/usePlatformShell.ts demo-web/src/platform/PlatformStatusSurface.tsx`
- [ ] `npm run lint`
</verification>

<success_criteria>

- Module packages are lazy-loaded from the artifact registry instead of eagerly loaded at app startup
- Shell state preserves active module and selected dataset cleanly across navigation
- Loading, unavailable, error, and deferred states are implemented once and reusable across baseline module pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-contracts-and-website-shell/07-02-SUMMARY.md`
</output>
