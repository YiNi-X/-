---
phase: 07-frontend-contracts-and-website-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-web/src/sharedContracts.ts
  - demo-web/src/runtimeSchemas.ts
  - demo-web/src/runtimeData.ts
  - demo-web/src/platform/moduleContracts.ts
  - demo-web/src/platform/moduleRegistry.ts
  - demo-web/src/platform/routeRegistry.ts
  - demo-web/tests/module-registry.test.mjs
autonomous: true
requirements:
  - BASE-01
  - BASE-03
must_haves:
  truths:
    - Every Phase 6 module package the shell will consume has a typed frontend contract, parsed registry metadata, and one explicit route descriptor before shell runtime work begins.
    - Module metadata centralizes label, status, manifest path, bundle path, deferred messaging, and shell ordering instead of scattering those decisions across pages.
    - The route and registry layer mirrors the locked Phase 7 shell ordering while still exposing overview as a first-class shell destination.
  artifacts:
    - demo-web/src/platform/moduleContracts.ts
    - demo-web/src/platform/moduleRegistry.ts
    - demo-web/src/platform/routeRegistry.ts
    - demo-web/src/runtimeSchemas.ts
    - demo-web/src/runtimeData.ts
    - demo-web/tests/module-registry.test.mjs
  key_links:
    - `artifact-index.json` becomes the single discovery surface for frontend module registration.
    - Runtime schemas validate registry, manifest, and bundle metadata before any page derives view state from Phase 6 artifacts.
    - Route descriptors encode the locked shell order for primary navigation and keep overview addressable through the shell without demoting it to hidden content.
---

<objective>
Define the frontend contract boundary for Phase 7 before building the new shell runtime.

Purpose: give the shell one reliable source of module truth and one explicit route model that later plans can consume without hardcoded bundle paths or ad hoc deferred logic.
Output: typed module contracts, validated registry/manifests/bundle loaders, and route descriptors for all Phase 6 module packages plus the forward-looking analysis placeholder.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-contracts-and-website-shell/07-CONTEXT.md
@.planning/phases/07-frontend-contracts-and-website-shell/07-RESEARCH.md
@demo-web/src/sharedContracts.ts
@demo-web/src/runtimeData.ts
@demo-web/src/runtimeSchemas.ts
@demo-web/public/data/modules/artifact-index.json
@demo-web/public/data/modules/forecast/manifest.json
@demo-web/public/data/modules/repair/manifest.json
@demo-web/public/data/modules/clustering/manifest.json
@demo-web/public/data/modules/evaluation/manifest.json
@demo-web/public/data/modules/overview/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validated runtime contracts for Phase 6 module registry and manifest surfaces</name>
  <files>demo-web/src/runtimeSchemas.ts, demo-web/src/runtimeData.ts, demo-web/src/sharedContracts.ts, demo-web/tests/module-registry.test.mjs</files>
  <action>Extend the runtime contract layer so the frontend can validate `artifact-index.json`, module manifests, and bundle metadata using the same strict parser approach already used for dashboard payloads. Add the shared TypeScript types needed to represent module registry entries, manifest artifacts, deferred item metadata, and bundle entry files. Back that work with a focused node test that proves the shipped Phase 6 registry and manifests parse correctly.</action>
  <verify>node --test demo-web/tests/module-registry.test.mjs</verify>
  <done>The frontend can parse the Phase 6 module discovery files through explicit runtime contracts instead of untyped JSON access.</done>
</task>

<task type="auto">
  <name>Task 2: Create the module registry boundary and shell route descriptors</name>
  <files>demo-web/src/platform/moduleContracts.ts, demo-web/src/platform/moduleRegistry.ts, demo-web/src/platform/routeRegistry.ts, demo-web/tests/module-registry.test.mjs</files>
  <action>Introduce one frontend module-registry layer that reads the validated artifact index, normalizes module metadata into shell-friendly descriptors, and defines the navigation entries for home, overview, forecast, repair, clustering, evaluation, and forward-looking analysis. Make the route order, display labels, first-class versus placeholder status, and deferred messaging explicit so later plans do not infer them from scattered UI code.</action>
  <verify>rg "forward-looking|overview|forecast|repair|clustering|evaluation|home" demo-web/src/platform/routeRegistry.ts</verify>
  <done>The shell has one canonical registry and route model that matches the locked Phase 7 information architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Add the page-facing contract adapters that protect later pages from raw JSON shape drift</name>
  <files>demo-web/src/platform/moduleContracts.ts, demo-web/src/platform/moduleRegistry.ts, demo-web/src/sharedContracts.ts</files>
  <action>Define the normalized contract boundary that page components will consume from registry and bundle metadata, including module readiness, entry files, deferred section descriptors, and summary-level fields that the shell will need before deeper module-specific parsing exists. Keep this layer intentionally narrow so Phase 7 pages can render consistent shells without binding directly to every raw JSON nuance.</action>
  <verify>npm run lint</verify>
  <done>Later Phase 7 shell work can consume one normalized contract layer instead of coupling every page to the raw bundle JSON structure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test demo-web/tests/module-registry.test.mjs`
- [ ] `rg "forward-looking|overview|forecast|repair|clustering|evaluation|home" demo-web/src/platform/routeRegistry.ts`
- [ ] `npm run lint`
</verification>

<success_criteria>

- Registry, manifest, and bundle metadata are covered by validated runtime contracts
- Shell route descriptors exist for every Phase 7 destination in the locked navigation order
- Later shell and page work can depend on one normalized module-contract layer instead of raw ad hoc JSON access
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-contracts-and-website-shell/07-01-SUMMARY.md`
</output>
