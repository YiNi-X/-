---
phase: 02-runtime-data-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-web/package.json
  - demo-web/package-lock.json
  - demo-web/src/runtimeSchemas.ts
  - demo-web/src/runtimeData.ts
  - demo-web/src/datasetCatalog.ts
  - demo-web/tests/smoke.test.mjs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - Malformed catalog, AIS playback, flow forecast, shared geometry, and corridor payloads are rejected before typed data reaches React state.
    - Dataset catalog loading no longer silently masks malformed shipped data behind the default catalog.
    - The repository has one shared runtime contract boundary that the dashboard and RouteEditor can both consume.
  artifacts:
    - demo-web/src/runtimeSchemas.ts
    - demo-web/src/runtimeData.ts
    - demo-web/src/datasetCatalog.ts
    - demo-web/tests/smoke.test.mjs
  key_links:
    - Runtime schema helpers are callable from both dashboard and RouteEditor loaders.
    - Catalog loading uses the shared validation boundary instead of a broad fallback catch.
    - Smoke checks assert the shipped demo assets still satisfy the structural contract surface.
---

<objective>
Create the shared runtime contract boundary for shipped JSON payloads.

Purpose: Phase 2 needs one place that decides whether runtime data is valid before UI state derives from it.
Output: Shared schema/loader modules, a strict catalog path, and stronger asset smoke coverage for committed demo payloads.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-data-contracts/02-CONTEXT.md
@.planning/phases/02-runtime-data-contracts/02-RESEARCH.md
@demo-web/package.json
@demo-web/src/datasetCatalog.ts
@demo-web/src/sharedContracts.ts
@demo-web/tests/smoke.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared runtime schemas for shipped payloads</name>
  <files>demo-web/package.json, demo-web/package-lock.json, demo-web/src/runtimeSchemas.ts</files>
  <action>Add `zod` as the only new runtime-validation dependency and create `runtimeSchemas.ts` with strict structural schemas for dataset catalog, shared geometry, AIS playback, flow forecast, and main corridor tracks. Keep the phase scoped to the website contract: validate curated corridor payloads, not raw research clustering artifacts. Treat IDs, study bounds, arrays, timestamps, and geometry as required. Only use controlled defaults for genuinely non-structural narrative/display strings where the UI already has deliberate fallback copy. Do not convert the whole app to inferred Zod types in this phase; keep the schema layer narrow so Phase 2 stays a safety pass, not a type-system rewrite.</action>
  <verify>cd demo-web && npm run build</verify>
  <done>`runtimeSchemas.ts` exports schema-backed parse helpers for every shipped runtime payload the dashboard and RouteEditor depend on, and the project installs cleanly with the new dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Build a structured runtime loader boundary</name>
  <files>demo-web/src/runtimeData.ts, demo-web/src/datasetCatalog.ts</files>
  <action>Create a small shared loader module that fetches JSON, validates it with the appropriate schema, and returns a structured success/failure result with resource kind, path, and human-readable contract detail. Update `datasetCatalog.ts` so malformed catalog payloads are rejected explicitly instead of silently returning `DEFAULT_DATASET_CATALOG` from a broad catch. Preserve `resolveRuntimeResource`, dataset selection, and path-format helpers so downstream surfaces can adopt the new boundary without a broader refactor. Keep UI policy out of this layer: the loader should explain what failed, not decide how the screen renders the failure.</action>
  <verify>cd demo-web && npm run lint -- src/runtimeData.ts src/datasetCatalog.ts && npm run build</verify>
  <done>The repo has one shared runtime-loading boundary that can validate catalog and JSON payloads, and catalog corruption now surfaces as an explicit load failure instead of an implicit fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten shipped-asset smoke coverage around the new contracts</name>
  <files>demo-web/tests/smoke.test.mjs</files>
  <action>Expand the existing smoke test so it asserts the structural fields the new runtime contract boundary depends on for catalog, AIS playback, flow forecast, shared geometry, and corridor payloads. Keep this within the existing lightweight `node:test` setup; do not introduce a second test runner in Phase 2. The goal is to catch broken committed demo assets early and keep verification aligned with the shipped offline demo path.</action>
  <verify>cd demo-web && npm run test</verify>
  <done>The smoke suite fails when a committed runtime asset loses required structural fields for the dashboard or RouteEditor contract boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint -- src/runtimeSchemas.ts src/runtimeData.ts src/datasetCatalog.ts`
- [ ] `cd demo-web && npm run test`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- All tasks completed
- Shared runtime schemas exist for every shipped JSON payload used by the UI
- Catalog loading fails explicitly on malformed data instead of silently downgrading to the default catalog
- Smoke coverage reflects the structural contract the shipped demo now depends on
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-data-contracts/02-01-SUMMARY.md`
</output>
