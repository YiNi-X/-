---
phase: 02-runtime-data-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - demo-web/src/RouteEditor.tsx
  - demo-web/src/route-editor.css
autonomous: true
requirements:
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - RouteEditor never renders or exports corridor data derived from an invalid `main-corridor-tracks.json` payload.
    - When corridor runtime data is unavailable, RouteEditor says so directly and avoids half-working editing/export affordances.
    - The RouteEditor contract remains centered on curated corridor payloads, not research clustering artifacts.
  artifacts:
    - demo-web/src/RouteEditor.tsx
    - demo-web/src/route-editor.css
  key_links:
    - RouteEditor loads `main-corridor-tracks.json` through the shared validated loader from Plan 02-01.
    - Invalid corridor payloads clear selection/export state before UI rendering.
    - The unavailable treatment reuses RouteEditor visual language instead of introducing a separate demo-mode banner.
---

<objective>
Protect RouteEditor against malformed corridor payloads and make failure states explicit.

Purpose: The editing surface must stay honest and operable when curated corridor data is missing or broken.
Output: RouteEditor uses the shared contract boundary, resets stale editor state on failure, and renders a clear unavailable treatment.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-data-contracts/02-CONTEXT.md
@.planning/phases/02-runtime-data-contracts/02-RESEARCH.md
@.planning/phases/02-runtime-data-contracts/02-01-SUMMARY.md
@demo-web/src/RouteEditor.tsx
@demo-web/src/route-editor.css
@demo-web/src/sharedContracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate corridor payloads before editor state is populated</name>
  <files>demo-web/src/RouteEditor.tsx</files>
  <action>Swap the RouteEditor load effect over to the shared validated loader from Plan 02-01 for `main-corridor-tracks.json`. On validation failure, clear `tracksFile`, `tracks`, `defaultTracks`, selection state, and any derived export state so the editor cannot keep rendering stale data. Keep the contract focused on curated corridor payloads only; do not add notebook-cluster compatibility or fallback transformations.</action>
  <verify>cd demo-web && npm run build</verify>
  <done>RouteEditor only populates editor state from a valid corridor payload, and invalid runtime data cannot leave stale selections or export content behind.</done>
</task>

<task type="auto">
  <name>Task 2: Replace warning-only editor fallback with a direct unavailable state</name>
  <files>demo-web/src/RouteEditor.tsx, demo-web/src/route-editor.css</files>
  <action>Turn the current missing-file note and raw warning paragraph into a direct unavailable presentation that matches the locked decisions: explicit data unavailability, product-like tone, and no explicit demo-mode framing. Keep the page readable for demo operators by clearly showing what file is missing or invalid, while disabling or visually de-emphasizing editing/export affordances that depend on valid corridor data. Do not invent replacement geometry when the curated payload is broken.</action>
  <verify>cd demo-web && npm run lint -- src/RouteEditor.tsx && npm run test && npm run build</verify>
  <done>When `main-corridor-tracks.json` is missing or malformed, RouteEditor presents a direct unavailable state and no longer looks partially editable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint -- src/RouteEditor.tsx`
- [ ] `cd demo-web && npm run test`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- All tasks completed
- Invalid corridor payloads do not populate RouteEditor state or exports
- RouteEditor communicates corridor-data failure directly instead of relying on a raw warning line
- The editor remains aligned with the curated main-corridor website contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-data-contracts/02-03-SUMMARY.md`
</output>
