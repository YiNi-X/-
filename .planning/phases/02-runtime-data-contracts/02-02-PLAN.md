---
phase: 02-runtime-data-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - demo-web/src/App.tsx
  - demo-web/src/App.css
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
must_haves:
  truths:
    - The dashboard never derives scene, geometry, or forecast UI state from invalid catalog, AIS, forecast, or geometry payloads.
    - When required dashboard data is missing or malformed, the page shows a clear `data unavailable` style state instead of pretending to be live.
    - Dataset selection continues to work for valid payloads without broadening the phase into a dashboard refactor.
  artifacts:
    - demo-web/src/App.tsx
    - demo-web/src/App.css
  key_links:
    - Dashboard effects call the shared validated loaders from Plan 02-01.
    - Invalid payload results clear stale dashboard state before rendering derived views.
    - Dashboard error styling communicates failure in a product-like way rather than as a demo-only warning.
---

<objective>
Wire the dashboard through the validated runtime contract boundary and give it an honest unavailable state.

Purpose: The main surface must fail safely when catalog, AIS, forecast, or geometry data is broken.
Output: Dashboard loading effects use validated runtime loaders, reset stale state on failure, and render a product-like unavailable treatment.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-data-contracts/02-CONTEXT.md
@.planning/phases/02-runtime-data-contracts/02-RESEARCH.md
@.planning/phases/02-runtime-data-contracts/02-01-SUMMARY.md
@demo-web/src/App.tsx
@demo-web/src/App.css
@demo-web/src/datasetCatalog.ts
@demo-web/src/sharedContracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw dashboard fetches with validated runtime loaders</name>
  <files>demo-web/src/App.tsx</files>
  <action>Update the dashboard loading effects so catalog, shared geometry, AIS playback, and flow forecast all enter `App.tsx` through the shared validated loader boundary from Plan 02-01. When any required dashboard payload fails validation, clear the dependent state (`aisPlayback`, `flowForecast`, geometry-derived state, and any dataset error that would otherwise leave stale scenes behind) before rendering. Keep the Phase 2 scope tight: do not split `App.tsx` into new hooks or component modules yet unless a tiny helper is unavoidable for readability.</action>
  <verify>cd demo-web && npm run build</verify>
  <done>`App.tsx` no longer calls unchecked JSON loaders for dashboard-critical payloads, and invalid runtime data cannot leak into derived scene state.</done>
</task>

<task type="auto">
  <name>Task 2: Add a direct dashboard unavailable state with product-like copy</name>
  <files>demo-web/src/App.tsx, demo-web/src/App.css</files>
  <action>Replace the current raw warning-only behavior with an explicit unavailable presentation that matches the locked decisions: fail directly, say the data is unavailable, and keep the tone like a normal software surface rather than a labeled demo mode. Use the existing dashboard layout language where possible so the page still feels intentional. Avoid fake live metrics when the required payloads are invalid; the unavailable state should be honest, visually clear, and easy for demo operators to explain.</action>
  <verify>cd demo-web && npm run lint -- src/App.tsx && npm run test && npm run build</verify>
  <done>Broken dashboard runtime data results in a visible unavailable state with no misleading live-derived metrics, while valid data still renders the existing dashboard behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint -- src/App.tsx`
- [ ] `cd demo-web && npm run test`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- All tasks completed
- Invalid catalog, AIS, forecast, or geometry payloads do not propagate into dashboard derived state
- Dashboard shows a direct unavailable treatment instead of only a buried warning string
- Valid offline demo datasets continue to render without changing the product story
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-data-contracts/02-02-SUMMARY.md`
</output>
