---
phase: 04-data-pipeline-hardening
plan: 01
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - README.md
  - demo-web/README.md
  - demo-web/DATA_PIPELINE.md
  - demo-web/requirements-pipeline.txt
autonomous: true
requirements:
  - PIPE-01
must_haves:
  truths:
    - A contributor can set up the Python environment and discover the supported regeneration commands without reading notebooks or source internals.
    - The repo documents the two official Python entrypoints and the exact boundary between provenance inputs, analysis outputs, and website runtime outputs.
    - The documented commands match the hardened CLI surface instead of stale pre-hardening behavior.
  artifacts:
    - demo-web/DATA_PIPELINE.md
    - demo-web/requirements-pipeline.txt
    - demo-web/README.md
  key_links:
    - The pipeline guide maps archived `代码依据/` assets to `demo-web/scripts/extract_main_corridors_from_clustered_ais.py` and `demo-web/scripts/generate_first_version_data.py`.
    - Repo docs point contributors to the pipeline guide instead of treating notebooks as the execution interface.
---

<objective>
Document the hardened Python environment, inputs, and regeneration commands for the offline demo pipeline.

Purpose: make the supported code path from archived source artifacts to website-ready payloads discoverable and repeatable for contributors.
Output: one contributor-facing pipeline guide, one checked-in Python dependency manifest, and updated repo entry docs that point to the official commands.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-pipeline-hardening/04-CONTEXT.md
@.planning/phases/04-data-pipeline-hardening/04-RESEARCH.md
@README.md
@demo-web/README.md
@demo-web/scripts/extract_main_corridors_from_clustered_ais.py
@demo-web/scripts/generate_first_version_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the contributor-facing pipeline guide and dependency manifest</name>
  <files>demo-web/DATA_PIPELINE.md, demo-web/requirements-pipeline.txt</files>
  <action>Inventory the Python dependencies actually required by the two supported regeneration scripts and write a checked-in dependency manifest for that flow. Create `demo-web/DATA_PIPELINE.md` as the single contributor guide for Phase 4: document supported Python version expectations, where commands are run from, the official `--help` and dry-run commands, the required archived input assets, and the stable runtime or analysis outputs each command is expected to produce. Treat notebooks under `代码依据/` as provenance only, not as supported execution steps.</action>
  <verify>python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --help && python demo-web/scripts/generate_first_version_data.py --help</verify>
  <done>The repo has one explicit Python setup surface and one pipeline guide that names the supported commands, assets, and output locations.</done>
</task>

<task type="auto">
  <name>Task 2: Update repo entry docs to point at the hardened pipeline workflow</name>
  <files>README.md, demo-web/README.md, demo-web/DATA_PIPELINE.md</files>
  <action>Update the top-level and `demo-web` READMEs so contributors can quickly find the new pipeline guide. Add concise scope language that distinguishes website runtime assets in `public/data/` from diagnostic artifacts in `analysis/`, and call out that archived assets under `代码依据/` are the local provenance inputs for regeneration. Keep the README updates compact and link outward to the dedicated pipeline guide for the detailed command table.</action>
  <verify>rg "DATA_PIPELINE|main-corridor-tracks|public/data|analysis" README.md demo-web/README.md demo-web/DATA_PIPELINE.md</verify>
  <done>A contributor landing in the repo can find the official Phase 4 pipeline commands and understand which files are runtime-facing versus research-facing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --help`
- [ ] `python demo-web/scripts/generate_first_version_data.py --help`
- [ ] `python -m compileall demo-web/scripts`
</verification>

<success_criteria>

- The repo has one dedicated pipeline guide and one checked-in Python dependency manifest
- Supported commands point to the hardened script entrypoints rather than notebooks
- Runtime outputs, analysis artifacts, and provenance inputs are explicitly separated in the docs
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-pipeline-hardening/04-01-SUMMARY.md`
</output>
