---
phase: 04-data-pipeline-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-web/scripts/pipeline_config.py
  - demo-web/scripts/pipeline_common.py
  - demo-web/scripts/extract_main_corridors_from_clustered_ais.py
  - demo-web/scripts/generate_first_version_data.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - Official pipeline defaults for inputs, thresholds, and output targets live in one versioned config surface instead of scattered magic values.
    - Both supported scripts can explain which paths they resolved and fail with actionable guidance before mutating trusted runtime outputs.
    - Contributors can inspect and override the regeneration surface through CLI help instead of editing Python constants directly.
  artifacts:
    - demo-web/scripts/pipeline_config.py
    - demo-web/scripts/pipeline_common.py
    - demo-web/scripts/generate_first_version_data.py
    - demo-web/scripts/extract_main_corridors_from_clustered_ais.py
  key_links:
    - `generate_first_version_data.py` consumes shared config and path-validation helpers rather than ad hoc repo-wide search.
    - `extract_main_corridors_from_clustered_ais.py` uses the same shared resolution and validation boundary for default inputs and outputs.
---

<objective>
Add centralized config and fail-fast validation to the supported Python regeneration scripts.

Purpose: make the offline pipeline reproducible, reviewable, and safer to run against the committed website output targets.
Output: shared config and helper modules plus hardened CLIs for corridor extraction and demo-data generation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-pipeline-hardening/04-CONTEXT.md
@.planning/phases/04-data-pipeline-hardening/04-RESEARCH.md
@demo-web/scripts/extract_main_corridors_from_clustered_ais.py
@demo-web/scripts/generate_first_version_data.py
@demo-web/scripts/stgcn_runtime.py
@demo-web/public/data/shared-geometry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a shared pipeline config and resolution layer</name>
  <files>demo-web/scripts/pipeline_config.py, demo-web/scripts/pipeline_common.py, demo-web/scripts/extract_main_corridors_from_clustered_ais.py, demo-web/scripts/generate_first_version_data.py</files>
  <action>Create one shared Python config surface for official archived inputs, website-owned inputs, stable output targets, and key thresholds or window defaults. Add helper functions for path resolution, compatibility fallback reporting, and actionable validation errors. Every missing-path error should name the asset, the attempted path or pattern, and the override flag or config field that can fix it. Keep `public/data/` and `analysis/` as the stable default output roots.</action>
  <verify>python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --help && python demo-web/scripts/generate_first_version_data.py --help</verify>
  <done>Both entrypoint scripts resolve their defaults through a shared config and helper boundary instead of independent hidden constants.</done>
</task>

<task type="auto">
  <name>Task 2: Harden corridor extraction around explicit validation and output ownership</name>
  <files>demo-web/scripts/pipeline_config.py, demo-web/scripts/pipeline_common.py, demo-web/scripts/extract_main_corridors_from_clustered_ais.py</files>
  <action>Refit the corridor extraction script so its defaults come from the shared config surface and its CLI clearly distinguishes provenance inputs, analysis outputs, and RouteEditor runtime output. Add a non-destructive validation or dry-run mode, report which source artifacts were resolved, and validate required inputs before writing files. Preserve the current runtime target name `main-corridor-tracks.json`, but make the script's logs and argument names explicit about that handoff from research clustering artifacts to website runtime payload.</action>
  <verify>python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --dry-run</verify>
  <done>Corridor extraction can preflight its inputs, explain what it will write, and fail before partial output mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Harden demo runtime generation around shared config and staged writes</name>
  <files>demo-web/scripts/pipeline_config.py, demo-web/scripts/pipeline_common.py, demo-web/scripts/generate_first_version_data.py</files>
  <action>Add `argparse` to `generate_first_version_data.py` and route its thresholds, demo window settings, source paths, and output targets through the shared config module. Support explicit CLI overrides for AIS source, flow CSV, adjacency CSV, model weights, geometry config, and output directory targets, plus a non-destructive validation or dry-run mode. Validate all required assets before generation starts and stage website-facing output writes so `public/data/` is only replaced after the run completes coherently enough to trust.</action>
  <verify>python demo-web/scripts/generate_first_version_data.py --dry-run</verify>
  <done>The runtime-data generator exposes a contributor-facing CLI, validates inputs up front, and no longer relies on scattered constants plus silent repo search.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --help`
- [ ] `python demo-web/scripts/generate_first_version_data.py --help`
- [ ] `python demo-web/scripts/extract_main_corridors_from_clustered_ais.py --dry-run`
- [ ] `python demo-web/scripts/generate_first_version_data.py --dry-run`
- [ ] `python -m compileall demo-web/scripts`
</verification>

<success_criteria>

- Shared config covers the official defaults for inputs, thresholds, and output targets
- Both entrypoint scripts fail fast with actionable missing-asset guidance
- Contributors can override the pipeline surface through CLI flags instead of editing script internals
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-pipeline-hardening/04-02-SUMMARY.md`
</output>
