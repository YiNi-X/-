---
phase: 03-ui-maintainability-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-web/src/App.tsx
  - demo-web/src/dashboard/dashboardUtils.ts
  - demo-web/src/dashboard/useDashboardRuntime.ts
  - demo-web/src/dashboard/useDashboardScene.ts
  - demo-web/src/dashboard/DashboardStatusScreen.tsx
autonomous: true
requirements:
  - MAINT-01
  - MAINT-03
must_haves:
  truths:
    - Dashboard runtime loading can change without editing one large `App.tsx` block.
    - Dashboard scene derivation and playback shaping are isolated from the top-level JSX shell.
    - Runtime unavailability handling stays behaviorally consistent after the refactor.
  artifacts:
    - demo-web/src/dashboard/useDashboardRuntime.ts
    - demo-web/src/dashboard/useDashboardScene.ts
    - demo-web/src/App.tsx
  key_links:
    - `App.tsx` composes extracted hooks instead of owning all data effects and scene memos directly.
    - Shared runtime validation from Phase 2 is consumed through focused dashboard modules, not inline fetch logic.
---

<objective>
Extract dashboard runtime loading and derived scene state out of `App.tsx`.

Purpose: make future dashboard behavior changes safer by separating loading, domain derivation, and fallback presentation from the top-level page shell.
Output: dedicated dashboard utility and hook modules, plus a slimmer `App.tsx` that composes them.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-maintainability-refactor/03-CONTEXT.md
@demo-web/src/App.tsx
@demo-web/src/datasetCatalog.ts
@demo-web/src/runtimeData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract dashboard helpers and runtime orchestration</name>
  <files>demo-web/src/dashboard/dashboardUtils.ts, demo-web/src/dashboard/useDashboardRuntime.ts, demo-web/src/App.tsx</files>
  <action>Move dashboard-only constants, geometry helpers, chart helpers, and shared formatting helpers into a feature utility module. Create a runtime hook that owns dataset catalog, shared geometry, AIS playback, and forecast loading, along with dataset selection and loading-state derivation. Keep the Phase 2 runtime contract boundary intact and do not reintroduce silent fallback behavior.</action>
  <verify>cd demo-web && npm run lint -- src/App.tsx src/dashboard/dashboardUtils.ts src/dashboard/useDashboardRuntime.ts</verify>
  <done>Dashboard runtime effects and helper functions no longer live inline in `App.tsx`.</done>
</task>

<task type="auto">
  <name>Task 2: Extract dashboard scene derivation and status presentation</name>
  <files>demo-web/src/dashboard/useDashboardScene.ts, demo-web/src/dashboard/DashboardStatusScreen.tsx, demo-web/src/App.tsx</files>
  <action>Move playback scene derivation, hotspot shaping, route/track projection, timeline construction, and chart-ready view data into a dedicated dashboard hook. Keep `App.tsx` responsible for local UI control state and final JSX composition only. If it meaningfully reduces noise, move the runtime loading/unavailable shell into a small presentational component with explicit props.</action>
  <verify>cd demo-web && npm run build</verify>
  <done>Future dashboard data-flow or derived-state changes can be made without reopening a single monolithic component file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint -- src/App.tsx src/dashboard/dashboardUtils.ts src/dashboard/useDashboardRuntime.ts src/dashboard/useDashboardScene.ts src/dashboard/DashboardStatusScreen.tsx`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- `App.tsx` no longer owns runtime loading effects inline
- Dashboard playback and scene derivation live in focused modules
- Fallback loading or unavailable behavior stays intact
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-maintainability-refactor/03-01-SUMMARY.md`
</output>
