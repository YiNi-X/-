---
phase: 03-ui-maintainability-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - demo-web/src/App.tsx
  - demo-web/src/RouteEditor.tsx
  - demo-web/src/dashboard/dashboardUtils.ts
  - demo-web/src/route-editor/routeEditorUtils.ts
autonomous: true
requirements:
  - MAINT-03
must_haves:
  truths:
    - Entry components act as composition shells over focused feature modules.
    - Reused runtime-loading and feature helper boundaries are clear enough that future work does not need to rediscover them in giant files.
    - The refactor preserves current dashboard and RouteEditor behavior under the existing verification commands.
  artifacts:
    - demo-web/src/App.tsx
    - demo-web/src/RouteEditor.tsx
  key_links:
    - Both entry surfaces consume extracted modules instead of duplicating large inline orchestration blocks.
    - Verification covers lint, test, and build after the maintainability refactor lands.
---

<objective>
Consolidate the maintainability refactor and verify both entry surfaces stay stable.

Purpose: finish Phase 3 by making the new boundaries explicit in the composition shells and verifying that the extracted modules preserve current behavior.
Output: cleaned entry components, pruned dead helpers, and full verification results.
</objective>

<context>
@.planning/phases/03-ui-maintainability-refactor/03-CONTEXT.md
@.planning/phases/03-ui-maintainability-refactor/03-01-PLAN.md
@.planning/phases/03-ui-maintainability-refactor/03-02-PLAN.md
@demo-web/src/App.tsx
@demo-web/src/RouteEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean composition shells and remove dead inline helpers</name>
  <files>demo-web/src/App.tsx, demo-web/src/RouteEditor.tsx, demo-web/src/dashboard/dashboardUtils.ts, demo-web/src/route-editor/routeEditorUtils.ts</files>
  <action>After the feature hooks land, prune dead inline helpers, simplify entry-component imports, and make the composition boundary readable. Keep this cleanup constrained to maintainability concerns; do not sneak in unrelated product changes.</action>
  <verify>cd demo-web && npm run lint</verify>
  <done>The entry components read as high-level composition shells over extracted modules.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the refactor end to end</name>
  <files>demo-web/src/App.tsx, demo-web/src/RouteEditor.tsx</files>
  <action>Run the existing verification commands for the brownfield demo and fix any integration regressions introduced by the refactor. Record the results in Phase 3 summaries and update project state if the full phase is complete.</action>
  <verify>cd demo-web && npm run verify</verify>
  <done>The maintainability refactor passes the current lint, test, and build baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint`
- [ ] `cd demo-web && npm run test`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- Dashboard and RouteEditor now compose focused modules instead of carrying most behavior inline
- Shared runtime/resource boundaries remain clear after the refactor
- Existing verification commands pass after the refactor
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-maintainability-refactor/03-03-SUMMARY.md`
</output>
