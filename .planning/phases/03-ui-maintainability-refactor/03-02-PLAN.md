---
phase: 03-ui-maintainability-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-web/src/RouteEditor.tsx
  - demo-web/src/route-editor/routeEditorUtils.ts
  - demo-web/src/route-editor/useRouteEditorWorkspace.ts
  - demo-web/src/route-editor/useRouteEditorStage.ts
  - demo-web/src/route-editor/RouteEditorStatusScreen.tsx
autonomous: true
requirements:
  - MAINT-02
  - MAINT-03
must_haves:
  truths:
    - RouteEditor pan, zoom, export, and point-editing behavior can change without editing one large `RouteEditor.tsx` block.
    - RouteEditor loading and selection logic stay isolated from sidebar markup.
    - Cleaned corridor payload handling still flows through the shared runtime contract boundary.
  artifacts:
    - demo-web/src/route-editor/useRouteEditorWorkspace.ts
    - demo-web/src/route-editor/useRouteEditorStage.ts
    - demo-web/src/RouteEditor.tsx
  key_links:
    - Stage interaction logic is separate from corridor selection and export logic.
    - `RouteEditor.tsx` becomes the composition shell for workspace state plus presentational markup.
---

<objective>
Extract RouteEditor interaction, selection, and export logic out of `RouteEditor.tsx`.

Purpose: make point editing, stage transforms, and export behavior safer to modify in isolation.
Output: dedicated RouteEditor utility and hook modules, plus a slimmer `RouteEditor.tsx` that composes them.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-maintainability-refactor/03-CONTEXT.md
@demo-web/src/RouteEditor.tsx
@demo-web/src/runtimeData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract corridor workspace state and editing behavior</name>
  <files>demo-web/src/route-editor/routeEditorUtils.ts, demo-web/src/route-editor/useRouteEditorWorkspace.ts, demo-web/src/RouteEditor.tsx</files>
  <action>Create a RouteEditor workspace hook that owns corridor loading, selected corridor or track derivation, selected point logic, export payload generation, point nudging, reset behavior, and transient copy status. Move track and geometry helpers into a feature utility module. Keep the current cleaned-track behavior and exported schema unchanged.</action>
  <verify>cd demo-web && npm run lint -- src/RouteEditor.tsx src/route-editor/routeEditorUtils.ts src/route-editor/useRouteEditorWorkspace.ts</verify>
  <done>RouteEditor domain behavior becomes editable without reopening one monolithic component file.</done>
</task>

<task type="auto">
  <name>Task 2: Extract stage transforms and runtime fallback presentation</name>
  <files>demo-web/src/route-editor/useRouteEditorStage.ts, demo-web/src/route-editor/RouteEditorStatusScreen.tsx, demo-web/src/RouteEditor.tsx</files>
  <action>Create a stage hook that owns cursor updates, active layer transforms, pan and zoom behavior, canvas or map asset controls, and blob URL cleanup. If it reduces noise, move the runtime loading/unavailable shell into a small presentational component with explicit props. Preserve current interaction behavior and keep `RouteEditor.tsx` focused on composition.</action>
  <verify>cd demo-web && npm run build</verify>
  <done>Stage interaction and loading-status behavior are separated cleanly from the RouteEditor page shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd demo-web && npm run lint -- src/RouteEditor.tsx src/route-editor/routeEditorUtils.ts src/route-editor/useRouteEditorWorkspace.ts src/route-editor/useRouteEditorStage.ts src/route-editor/RouteEditorStatusScreen.tsx`
- [ ] `cd demo-web && npm run build`
</verification>

<success_criteria>

- `RouteEditor.tsx` no longer owns all interaction and selection logic inline
- Point editing, exports, and stage transforms live in focused modules
- RouteEditor runtime fallback behavior stays intact
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-maintainability-refactor/03-02-SUMMARY.md`
</output>
