---
phase: 09-trajectory-repair-module-completion
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - demo-web/src/platform/pages/RepairPage.tsx
  - demo-web/src/platform/repair/repairTypes.ts
  - demo-web/src/platform/repair/repairViewModel.ts
  - demo-web/src/platform/repair/RepairPrimaryStage.tsx
  - demo-web/src/platform/repair/RepairDetailGrid.tsx
  - demo-web/src/platform/repair/RepairSummaryBand.tsx
  - demo-web/src/App.css
  - demo-web/package.json
  - demo-web/tests/repair-module.test.mjs
autonomous: true
requirements:
  - REPR-04
  - REPR-05
  - EVAL-03
  - BASE-03
must_haves:
  truths:
    - The worktree already contains a real repair bundle and a usable repair cockpit, so 09-03 should finish and verify that experience instead of restarting the module from zero.
    - The repair UI must expose the full metric-summary story promised by `REPR-05`, including `MAE`, `RMSE`, `DTW`, `R-squared`, and `Hausdorff`, while staying honest that the evidence is sample-scoped and offline-derived.
    - Repair-specific smoke coverage must defend the landed sample switching, model switching, trajectory overlay, error-chart, and metric-summary behavior instead of relying only on shell-level tests.
  artifacts:
    - .planning/phases/09-trajectory-repair-module-completion/09-CONTEXT.md
    - demo-web/src/platform/pages/RepairPage.tsx
    - demo-web/src/platform/repair/repairTypes.ts
    - demo-web/src/platform/repair/repairViewModel.ts
    - demo-web/src/platform/repair/RepairPrimaryStage.tsx
    - demo-web/src/platform/repair/RepairDetailGrid.tsx
    - demo-web/public/data/modules/repair/repair-bundle.json
    - demo-web/public/data/modules/repair/repair-metrics.json
  key_links:
    - `09-CONTEXT.md` already treats `09-01` and `09-02` as effectively landed in the worktree and frames `09-03` as the remaining finish pass.
    - `repair-metrics.json` already contains `ade` and `r2`, so the likely remaining gap is UI exposure and verification rather than data generation.
    - `RepairDetailGrid.tsx` and `RepairPrimaryStage.tsx` already surface part of the metric story; 09-03 should turn that into a complete, demo-ready summary layer.
---

<objective>
Close the remaining Phase 9 gap by finishing the repair detail layer, exposing the full metric summary promised by the roadmap, and adding repair-specific verification coverage.

Purpose: turn the already-landed repair bundle and baseline cockpit into a fully defensible module that clearly satisfies `REPR-04` and `REPR-05` without reopening the data-export scope unless a real mismatch is found.
Output: a repair module whose error charts, metric summaries, and smoke coverage match the intended Phase 9 finish line.
</objective>

<execution_context>
@C:/Users/X/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/X/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-trajectory-repair-module-completion/09-CONTEXT.md
@demo-web/src/platform/pages/RepairPage.tsx
@demo-web/src/platform/repair/useRepairModule.ts
@demo-web/src/platform/repair/repairTypes.ts
@demo-web/src/platform/repair/repairViewModel.ts
@demo-web/src/platform/repair/RepairPrimaryStage.tsx
@demo-web/src/platform/repair/RepairDetailGrid.tsx
@demo-web/src/platform/repair/RepairSummaryBand.tsx
@demo-web/public/data/modules/repair/repair-bundle.json
@demo-web/public/data/modules/repair/repair-metrics.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the repair view-model and metric-summary contract for the Phase 9 detail layer</name>
  <files>demo-web/src/platform/repair/repairTypes.ts, demo-web/src/platform/repair/repairViewModel.ts, demo-web/src/platform/repair/RepairPrimaryStage.tsx, demo-web/src/platform/repair/RepairDetailGrid.tsx, demo-web/src/platform/repair/RepairSummaryBand.tsx</files>
  <action>Extend the repair view model and supporting components so the selected sample and selected model expose one complete metric-summary surface. Ensure the UI clearly presents the roadmap-promised metrics, especially `R-squared` and `ADE`, alongside the already-visible `MAE`, `RMSE`, `DTW`, and `Hausdorff` values. Keep the current sample-scoped, notebook-derived framing explicit rather than implying a live replay or online inference flow.</action>
  <verify>rg "R-squared|R2|ADE|Hausdorff|DTW|RMSE|MAE" demo-web/src/platform/repair demo-web/src/platform/pages/RepairPage.tsx</verify>
  <done>The repair cockpit exposes a complete and internally consistent metric-summary story for the selected sample and model.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the repair module's comparison surfaces and copy for demo readiness</name>
  <files>demo-web/src/platform/pages/RepairPage.tsx, demo-web/src/platform/repair/RepairPrimaryStage.tsx, demo-web/src/platform/repair/RepairDetailGrid.tsx, demo-web/src/platform/repair/RepairSummaryBand.tsx, demo-web/src/App.css</files>
  <action>Polish the repair module so the trajectory overlay, error chart, ranking panel, and summary cards read as one coherent repair cockpit rather than separate partial widgets. If needed, adjust layout and copy so the selected sample, selected model, visible layers, and currently highlighted error metric remain easy to understand on both wider and narrower screens.</action>
  <verify>rg "Trajectory Repair|Curated Repair Cockpit|Error Stage|Sample Ranking|Compare Metrics" demo-web/src/platform/repair demo-web/src/platform/pages/RepairPage.tsx</verify>
  <done>The repair page behaves like one demo-ready module with clear control context, readable metric emphasis, and honest offline framing.</done>
</task>

<task type="auto">
  <name>Task 3: Add repair-specific smoke coverage and wire it into the standard verification path</name>
  <files>demo-web/package.json, demo-web/tests/repair-module.test.mjs, demo-web/tests/platform-shell-smoke.test.mjs</files>
  <action>Add a repair-focused module test that asserts the curated sample catalog, model switching affordances, trajectory overlay structure, error-metric switching, and full metric-summary coverage expected by Phase 9. Update the smoke script so repair coverage becomes part of the standard verification path without dropping the existing shell and forecast checks.</action>
  <verify>node --test demo-web/tests/repair-module.test.mjs</verify>
  <done>The repair module has explicit repeatable coverage for the behaviors that define Phase 9 completion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg "R-squared|R2|ADE|Hausdorff|DTW|RMSE|MAE" demo-web/src/platform/repair demo-web/src/platform/pages/RepairPage.tsx`
- [ ] `rg "Trajectory Repair|Curated Repair Cockpit|Error Stage|Sample Ranking|Compare Metrics" demo-web/src/platform/repair demo-web/src/platform/pages/RepairPage.tsx`
- [ ] `node --test demo-web/tests/repair-module.test.mjs`
- [ ] `npm run smoke`
- [ ] `npm run verify`
</verification>

<success_criteria>

- The repair module clearly satisfies `REPR-04` and `REPR-05` in the shipped UI rather than only in the exported JSON
- Selected sample, model, trajectory overlay, error series, and metric summary feel like one coherent cockpit
- Repair-specific smoke coverage exists and is part of the normal verification path

</success_criteria>

<output>
After completion, create `.planning/phases/09-trajectory-repair-module-completion/09-03-SUMMARY.md`
</output>
